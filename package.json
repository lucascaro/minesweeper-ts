{
  "name": "minesweeper-ts",
  "version": "1.0.5",
  "description": "",
  "author": "Lucas Caro",
  "main": "dist/index.js",
  "bin": {
    "minesweeper-ts": "./bin/cli.sh"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "watch": "tsc --watch",
    "prepublishOnly": " npm run build",
    "release": "npm version patch && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.1.1"
  },
  "dependencies": {
    "@types/node": "^10.11.3"
  }
}
